pipeline {
    agent any
    tools {
        nodejs "NodeJS 18.0.0"
    }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
